namespace Tetrage.Audio
{
    /// <summary>
    /// InGame で使用する SE の識別子。
    /// </summary>
    public enum InGameSEId
    {
        GameStart = 0,
        CardMove = 1,
        CardFlip = 2,
        ButtonClick = 3,
    }
}
